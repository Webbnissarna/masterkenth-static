import { Theme } from "@theme-ui/css";

const theme: Theme = {
  colors: {
    text: "#333",
    background: "#cff",
  },
  space: [0],

  styles: {
    root: {
      margin: 0,
      padding: 0,
    },
  },
};

export default theme;
